export const meta = {
  title: '컴파일러 vs 인터프리터: 왜 속도 차이가 날까?',
  summary:
    '컴파일 언어와 인터프리터 언어의 근본적인 원리 차이와, 실행 횟수에 따라 속도가 달라지는 이유를 정리합니다.',
  tech: 'General',
  slug: '/compiler-vs-interpreter',
  type: 'Post',
  tags: ['컴파일러', '인터프리터', '실행 성능', '언어 런타임'],
}

# 컴파일러 vs 인터프리터: 왜 속도 차이가 날까?

프로그래밍 언어는 인간이 이해하기 쉬운 **고급 언어(High-level language)** 로 작성된다.  
하지만 컴퓨터는 고급 언어를 직접 실행할 수 없기 때문에, **기계어로 번역하는 과정**이 반드시 필요하다.  
이 번역을 담당하는 것이 바로 **컴파일러(Compiler)** 혹은 **인터프리터(Interpreter)**다.

이 글에서는 단순한 개념 설명을 넘어,  
**왜 이 둘의 실행 속도 차이가 발생하는지**,  
**실행 횟수에 따라 어떤 방식이 유리해지는지**를 원리부터 이해하기 쉽게 설명한다.

---

## 1. 컴파일러와 인터프리터의 기본 개념

### 🔹 컴파일러(Compiler)
- 실행 전에 **전체 소스코드**를 기계어로 변환하여 실행 파일을 만든다.
- 실행 중에는 번역 과정이 필요 없다.
- C, C++, Rust, Go 등이 컴파일 언어에 속한다.

### 🔹 인터프리터(Interpreter)
- **실행하면서** 코드를 한 줄씩(또는 문장별로) 해석하고 실행한다.
- 실행 중 번역 작업이 계속 필요하다.
- Python, JavaScript, Ruby 등이 인터프리터 기반 언어다.

---

## 2. 속도 차이가 생기는 근본 이유

핵심은 단 하나다.

> **컴파일러는 번역을 한 번만 하고, 인터프리터는 실행 중 내내 번역을 반복한다.**

### 🔸 컴파일러의 동작 구조
소스코드 → (1회 번역) → 기계어 실행파일 → CPU가 바로 실행
- 번역 비용은 "실행 전 1번" 발생
- 실행 중 번역 오버헤드는 사실상 0

### 🔸 인터프리터의 동작 구조
코드 읽기 → 해석 → 실행 → 다음 줄로 이동 → 반복...
- 실행할 때마다 계속 번역 비용이 발생
- 반복문에서는 같은 문장을 수천 번, 수백만 번 다시 해석해야 함

---

## 3. 시간복잡도 관점에서 구조적 차이 이해하기

엄밀한 시간복잡도는 아니지만 **개념을 설명하기에 좋은 비유**로 보면 다음과 같다.

### ✔ 컴파일러의 번역 비용 = O(1)
- 실행 전 번역을 단 한 번만 수행
- 프로그램 실행 횟수에 영향을 받지 않는다

### ✔ 인터프리터의 번역 비용 = O(n)
- `n = 프로그램 실행 중 수행되는 명령의 총 횟수`
- 실행할 때마다 해석 과정이 반복되므로 실행량에 비례해 증가

즉,

> **실행 횟수가 많아질수록 인터프리터는 선형적으로 느려지고,  
컴파일러는 처음 한 번만 비용이 들기 때문에 차이가 급격히 벌어진다.**

---

## 4. 실행 횟수에 따른 속도 비교

### 🔹 실행 횟수가 적을 때
- "Hello World" 같은 코드처럼 **딱 한 번만 실행되는 경우**
- 컴파일러는 컴파일 시간이 존재하므로  
  인터프리터와 속도 차이가 거의 없다  
  (심지어 인터프리터가 더 빠를 때도 있다)

### 🔹 실행 횟수가 많을 때
반복문이 많은 계산 코드의 경우:

```python
for i in range(1000000):
    x = i + 1
```

인터프리터는 이 문장을 100만 번 해석하여 실행해야 한다.  
반면 컴파일러는 이미 기계어로 번역된 명령을 CPU가 바로 100만 번 실행한다.

=> 이 지점부터는 컴파일러가 압도적으로 빨라진다.

## 5. 그래서 실제로 어떻게 선택해야 할까?

### ✔ 컴파일 방식이 유리한 경우

- 대규모 반복 연산
- 장기 실행되는 서버 프로세스
- 게임 엔진, 시뮬레이션, 데이터 처리
- 성능이 최우선인 시스템

### ✔ 인터프리터 방식이 유리한 경우

- 빠른 개발 및 테스트
- 간단한 자동화 스크립트
- 즉시 실행이 중요한 환경
- 브라우저 JavaScript처럼 빠른 피드백이 필요한 곳

---

## 6. 정리

컴파일러는 실행 전 1번 번역, 실행 중 오버헤드 거의 없음 → 실행이 빠름  
인터프리터는 실행 중 계속 번역, 반복될수록 비용 증가 → 느림  

실행 횟수가 적을 때는 둘의 차이가 거의 없지만  
반복이 많아질수록 컴파일러의 속도 우위는 절대적이다.

성능과 개발 효율성은 상황에 따라 선택이 달라진다.  
중요한 것은 왜 속도 차이가 발생하는지 원리를 이해하는 것,  
그것만 알면 어떤 언어/방식을 선택해야 할지 훨씬 명확해진다.